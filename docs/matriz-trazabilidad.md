# Matriz de trazabilidad

Ítem del instrumento de campo → Requerimiento funcional → Componente que lo implementa → Prueba que lo cubre.

| Ítem de encuesta / entrevista                                                            | RF                     | Componente (código)                                                                                                                                                                                                                                                                                                                                                 | Prueba                                                                                                                                                                          |
| ---------------------------------------------------------------------------------------- | ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Ítem 1–2 (forma de contratación actual; % de pérdidas por intermediación)                | RF-2.1, RF-2.3, RF-2.4 | `core/domain/tarifa.ts`, `core/application/tarifas/*`, rutas `/tarifas/reglas`, `/solicitudes/estimacion`                                                                                                                                                                                                                                                           | `tarifa.spec.ts`, `tarifas.int.spec.ts`                                                                                                                                         |
| "¿Y si el cultivo no es maíz ni banano?" (limitación detectada en pruebas)               | RF-2.3, RF-2.6         | catálogo `ReglasTarifa.cultivos` (shared), `tarifas/listar-cultivos.ts`, `TarifasComponent` (alta/baja), selector en `NuevaSolicitudComponent`                                                                                                                                                                                                                      | `tarifas.int.spec.ts` (alta de "arroz", rechazo de cultivo desconocido)                                                                                                         |
| "¿Cómo sé que ese transportista trabaja con la empresa?" (riesgo de defensa)             | RF-1.1, RF-1.6         | `rolRegistrableSchema` = solo productor; `identidad/gestion-transportistas.ts`, `despacho/gestion-vehiculos.ts` (alta/edición admin), rutas `/admin/transportistas`, `/admin/vehiculos`, `FlotaComponent` (`/a/flota`)                                                                                                                                              | `flota.int.spec.ts` (alta CONFIRMADA, duplicado 409, vehículo a transportista inexistente 422, editar zona/estado)                                                              |
| "¿A quién se asignó el flete y puedo reasignarlo?" (transparencia del admin)             | RF-4.6                 | `flete.transportistaNombre`/`vehiculoPlaca` (`asignar-flete.ts` + relleno perezoso en `consultar-fletes.ts`), `despacho/reasignar-flete.ts` (compone cancelar + `asignarFlete`), `POST /fletes/:id/reasignar`, `FletesAdminComponent` (estado → modal de detalle con contacto e historial, diálogo de reasignación, `FiltroChipsComponent` + `PaginacionComponent`) | `asignacion.int.spec.ts` (reasigna: cancela original, libera v1, ocupa v2, solicitud apunta al nuevo; rechaza zona incompatible sin tocar el flete)                             |
| "Si un transportista cancela un flete debe decir el porqué" (transparencia)              | RF-5.3                 | `cambiar-estado-flete.ts` (exige `motivo` al transportista, lo persiste en `flete.motivoCancelacion` y en el `timeline`), diálogo de cancelación en `MisFletesComponent`, motivo en el correo `EstadoFleteCambiado`                                                                                                                                                 | `monitoreo.int.spec.ts` (sin motivo ⇒ ValidationError; con motivo ⇒ guardado en flete, timeline y evento)                                                                       |
| Listas largas navegables (Mobile-First) — paginación y filtros reutilizables             | RNF-6, RNF-7           | `shared/paginacion.component.ts` (`paginar()` + `<app-paginacion>`), `shared/filtro-chips.component.ts`; usados en `MisSolicitudesComponent`, `MisFletesComponent`, `FlotaComponent`, `FletesAdminComponent`                                                                                                                                                        | `paginacion.util.spec.ts` (porciones, bordes, total de páginas)                                                                                                                 |
| Datos personales editables / cambio de contraseña                                        | RF-1.5                 | `identidad/actualizar-perfil.ts`, `identidad/cambiar-password.ts`, `PerfilComponent` (repite contraseña + ojito), `PasswordFieldComponent`, rutas `PATCH /perfil`, `POST /perfil/password`                                                                                                                                                                          | `identidad.int.spec.ts` (actualiza datos; exige contraseña actual)                                                                                                              |
| "¿Y si el vehículo se daña en ruta?" (limitación detectada en pruebas)                   | RF-5.1, RF-5.3b        | estado `INCIDENCIA` (shared), `monitoreo/registrar-incidencia.ts`, `PATCH /fletes/:id/incidencia`, diálogo en `MisFletesComponent`                                                                                                                                                                                                                                  | `monitoreo.int.spec.ts` (flete→INCIDENCIA, solicitud reencolada, vehículo INACTIVO/DISPONIBLE)                                                                                  |
| Ítem 5 (almacenamiento en acopios; saber cuánto grano hay) — roadmap #1                  | RF-7.1–7.5             | `core/domain/inventario.ts`, `core/application/inventario/*`, `dynamo/inventario.repository.ts`, subscriber `actualizar-stock`, `/a/inventario`                                                                                                                                                                                                                     | `inventario.int.spec.ts` (entrega suma stock; ajuste cruza umbral → StockBajo/StockAlto; tablero)                                                                               |
| Ítem 3–4 (reducir la espera de asignación; el portal coordina solo) — roadmap #2         | RF-4.5                 | `despacho/emparejar-automatico.ts`, subscriber `emparejar-automatico`, `ajustes/*` + `/a/ajustes`, marca `auto` en el flete                                                                                                                                                                                                                                         | `emparejamiento.int.spec.ts` (auto-asigna; sin vehículo → PENDIENTE; menor capacidad; interruptor OFF; no duplica con asignación manual)                                        |
| Ítem 3–4 (seguimiento en tiempo real que promete el artículo) — roadmap #3+#4            | RF-5.7                 | `monitoreo/registrar-ubicacion.ts`, `FleteRepository.agregarTrack/ruta`, rutas `POST /fletes/:id/ubicacion` y `GET /fletes/:id/ruta`, `MapaFleteComponent` (Leaflet + OSM), `tracking.util.ts`                                                                                                                                                                      | `monitoreo.int.spec.ts` (guarda punto + ruta ordenada; no-dueño 403; coord fuera de EC 422; flete cerrado 409), `tracking.util.spec.ts` (km restantes / progreso / ETA / señal) |
| "El transportista se puede olvidar de marcar la entrega" (limitación detectada)          | RF-5.8                 | `RoutingPort`/`adapters/geo/osrm`, `flete.rutaVial` en `asignar-flete.ts`, geocerca en `registrar-ubicacion.ts` (reusa `cambiarEstadoFlete`), círculo en `MapaFleteComponent`, `scripts/simular-ruta.ts`                                                                                                                                                            | `monitoreo.int.spec.ts` (guarda `rutaVial`; geocerca ⇒ ENTREGADO + solicitud COMPLETADA + `EntregaConfirmada`)                                                                  |
| "¿Y si no tengo GPS o no sé mis coordenadas?" (recogida por dirección)                   | RF-3.6                 | `GeocodingPort`/`adapters/geo/photon`, `geo/buscar-lugares.ts`, `GET /geo/buscar`, `BuscadorLugarComponent`, `origenNombre` en solicitud/flete                                                                                                                                                                                                                      | `buscar-lugares.spec.ts` (delega en el puerto; rechaza < 3 caracteres)                                                                                                          |
| Ítem 1–2 (cálculo por kilómetros y tipo de carga)                                        | RF-2.1, RF-2.2         | `haversineKm` (shared), `distanciaVialKm`, `calcularTarifa`                                                                                                                                                                                                                                                                                                         | `tarifa.spec.ts` (distancias conocidas, factores, temporada, redondeo)                                                                                                          |
| Ítem 3–4 (retrasos en horas para evacuar maíz/banano)                                    | RF-3.1, RF-5.5, RF-5.6 | `core/application/despacho/crear-solicitud.ts`, `monitoreo/detectar-retrasos.ts`, `metricas-operativas.ts`; worker `node-cron`                                                                                                                                                                                                                                      | `despacho.int.spec.ts`, `monitoreo.int.spec.ts` (retraso idempotente, métricas)                                                                                                 |
| Ítem 3–4 (portal que despacha alertas por eventos)                                       | RF-5.4, RF-6.1, RF-6.2 | outbox (`events/local-outbox`), `worker/dispatcher.ts`, `worker/subscribers.ts`                                                                                                                                                                                                                                                                                     | `identidad.int.spec.ts` (worker despacha correo), `monitoreo.int.spec.ts` (EntregaConfirmada en outbox)                                                                         |
| Ítem 5–6 (almacenamiento y cobertura móvil; enfoque Mobile-First)                        | RF-3.2, RNF-6, RNF-7   | `web/` Angular PWA, `NuevaSolicitudComponent` (≤ 3 toques), `ngsw-config.json`                                                                                                                                                                                                                                                                                      | build web + verificación manual del conteo de toques; `guion-demo.md`                                                                                                           |
| Entrevista — Eje Arquitectura (monolito → microservicios FaaS)                           | RNF-8, RNF-9           | `api/src/core` (puertos) + `adapters/` + `entrypoints/`; `infra/template.yaml`                                                                                                                                                                                                                                                                                      | `sam validate`; `arquitectura.md`                                                                                                                                               |
| Entrevista — Eje Rendimiento (Node.js, poda de dependencias, cold start)                 | RNF-1, RNF-2           | `pnpm --filter @agroflete/api build:bundle` (esbuild --minify), `scripts/src/coldstart.ts`, `scripts/src/load.ts`                                                                                                                                                                                                                                                   | `reporte-metricas.md` (carga 100 conc.; arranque 743 → 269 ms)                                                                                                                  |
| Entrevista — Eje Persistencia (NoSQL por transacciones bajo demanda)                     | RNF-3                  | DynamoDB single-table (`adapters/persistence/dynamo/*`), `infra/template.yaml` (PAY_PER_REQUEST)                                                                                                                                                                                                                                                                    | suites `*.int.spec.ts` contra DynamoDB Local                                                                                                                                    |
| Diagnóstico (72.5% coordina informal; 55% espera 6–12 h; 11–25% margen a intermediarios) | RF-2._, RF-4._, RF-5.6 | Flujo completo productor → asignación → entrega; `metricas-operativas.ts`                                                                                                                                                                                                                                                                                           | `asignacion.int.spec.ts`, `monitoreo.int.spec.ts`, seed de demo + `/a/metricas`                                                                                                 |
| Seguridad del software (validada en entrevistas)                                         | RNF-5                  | `authInterceptor`/`roleGuard` (web), `authJwt`/`requireRole` (api), `zod` en cada handler, `bcrypt`                                                                                                                                                                                                                                                                 | `token.service.spec.ts`, `password.hasher.spec.ts`, `identidad.int.spec.ts`, `app.spec.ts`                                                                                      |
| Tolerancia a fallos / módulos desacoplados                                               | RNF-4                  | outbox + reintentos + `registrarIntento`; SAM: SQS + DLQ                                                                                                                                                                                                                                                                                                            | `monitoreo.int.spec.ts` (idempotencia), diseño en `diagrama-eventos.md`                                                                                                         |
